class ContinueTest {
    def void Func() {
        int from, to;
        from = 1;
        to = 10;
        while (from < to) {
            from = from - 1;
            if (from == 4) {
                while (from != 0) {
                    from = from - 1;
                    if (from >= 1 && from <= 2) {
                        continue;
                    } else { ;
                    }
                }
                continue;
            } else {
                continue;
            }
        }
        return ;
    }

    def void main() {
        Func();
        return;
    }
}
